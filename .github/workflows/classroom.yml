name: Autograding Tests
'on':
- push
- repository_dispatch
permissions:
  checks: write
  actions: read
  contents: read
jobs:
  run-autograding-tests:
    runs-on: ubuntu-latest
    if: github.actor != 'github-classroom[bot]'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Validate text file
      id: validate-text-file
      uses: classroom-resources/autograding-command-grader@v1
      with:
        test-name: Validate text file
        setup-command: ''
        command: "./tests/validate_studentinfo.sh"
        timeout: 10
        max-score: 1
    - name: Autograding Reporter
      uses: classroom-resources/autograding-grading-reporter@v1
      env:
        VALIDATE-TEXT-FILE_RESULTS: "${{steps.validate-text-file.outputs.result}}"
      with:
        runners: validate-text-file
